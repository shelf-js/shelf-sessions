{
  "name": "shelf-sessions",
  "version": "0.1.0",
  "description": "A simple tool for defining user session model classes in redis using shelf",
  "main": "dist/index.js",
  "scripts": {
    "compile": "babel --ignore node_modules,dist -d dist/ --stage 0 .",
    "start": "npm run compile && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "standard --verbose --ignore dist/ | snazzy",
    "prepublish": "npm run compile"
  },
  "pre-commit": [
    "lint"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/jgantunes/shelf-sessions"
  },
  "keywords": [
    "redis",
    "model",
    "session",
    "user"
  ],
  "author": {
    "name": "João Antunes",
    "email": "j.goncalo.antunes@gmail.com"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jgantunes/shelf-sessions/issues"
  },
  "homepage": "https://github.com/jgantunes/shelf-sessions",
  "devDependencies": {
    "babel": "^5.8.20",
    "pre-commit": "^1.1.1",
    "snazzy": "^1.0.3",
    "standard": "^5.0.0"
  },
  "dependencies": {
    "shelf": "git://github.com/jgantunes/shelf.git"
  }
}
